import { useContext } from "react";
import ActivePhoneContext from "../context/activePhone/ActivePhoneContext";

const ActivePhoneData = () => useContext(ActivePhoneContext);

export default ActivePhoneData;
