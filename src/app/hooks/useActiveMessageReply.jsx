import { useContext } from "react";
import ActiveMessageContext from "../context/activeMessageReply/ActiveMessageReplyContext";

const ActiveMessageData = () => useContext(ActiveMessageContext);

export default ActiveMessageData;
