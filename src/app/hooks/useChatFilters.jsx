import { useContext } from "react";
import ChatFiltersContext from "../context/chatFilters/ChatFiltersContext";

const ChatFiltersData = () => useContext(ChatFiltersContext);

export default ChatFiltersData;
