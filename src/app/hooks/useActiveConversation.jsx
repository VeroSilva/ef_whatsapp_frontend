import { useContext } from "react";
import ActiveConversationContext from "../context/activeConversation/ActiveConversationContext";

const ActiveConversationData = () => useContext(ActiveConversationContext);

export default ActiveConversationData;
